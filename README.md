Minimax Algorithm in Tic-Tac-Toe

Key Features: Ensures unbeatable AI for the computer opponent.

Requirements -VS Code -Python 3.x -pygame

Installations Ensure Python is installed on system. It can be downloaded from python.org.

Install the required Python package using pip in the command prompt: pip3 install pygame

Code is Executed by being on runner.py and selecting run python file
